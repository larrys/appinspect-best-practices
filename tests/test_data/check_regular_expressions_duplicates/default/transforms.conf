[one]
REGEX = A:(?P<_KEY_1>\w+)\s*:\s*(?P<_VAL_1>\w+)

[two]
REGEX = A:(?P<_KEY_1>\w+)\s*:\s*(?P<_VAL_1>\w+)

[three]
REGEX = B:(?P<_KEY_2>\w+)\s*:\s*(?P<_VAL_2>\w+)

[four]
REGEX = B:(?<_KEY_2>\w+)\s*:\s*(?<_VAL_2>\w+)

[five]
REGEX = C:(?<_KEY_A>\w+)\s*:\s*(?<_VAL_A>\w+)

[six]
REGEX = C:(?<_KEY_B>\w+)\s*:\s*(?<_VAL_B>\w+)

[multi_value_with_quotation]
REGEX = ([^\s\=]+)="([^"]+)"
FORMAT = $1::$2
MV_ADD = True

[first_level_value_with_quotation]
REGEX = ([^\s\=]+)="([^"]+)"
FORMAT = $1::$2
MV_ADD = True

[seven]
REGEX = T(?P<_KEY_1>\w+)\s*:\s*(?P<_VAL_1>\w+)
